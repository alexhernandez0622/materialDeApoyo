// Enunciado: Crear una función que sume dos números.
// Función: sumar
// Parámetros: a (número), b (número)
// Retorno: La suma de a y b.

function sumar(a, b) {
    // Declaración de la función con dos parámetros: a y b.
    const resultado = a + b; 
    // Suma de los dos números.
    return resultado; 
    // Retorno del resultado.
  }
  
  // Uso de la función:
  const resultadoSuma = sumar(5, 3); 
  // Llamada a la función con los argumentos 5 y 3.
  console.log(resultadoSuma); 
  // Imprime el resultado en la consola.
  