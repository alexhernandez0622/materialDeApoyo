/*Crear un programa que me verifique si la persona es mayor de edad y vive en Nueva York */

// Ejemplo de operador AND (&&)
const edad = 25;
const ciudad = "Nueva York";

// Verificación si la persona es mayor de edad y vive en Nueva York
if (edad >= 18 && ciudad === "Nueva York") {
  console.log("La persona es mayor de edad y vive en Nueva York.");
} else {
  console.log("La persona no cumple con los criterios especificados.");
}
