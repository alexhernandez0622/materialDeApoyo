// Ejemplo 2: Función flecha que calcula el cuadrado de un número
// Función flecha que eleva un número al cuadrado
const alCuadrado = (num) => Math.pow(num, 2);
console.log(alCuadrado(5)); // Output: 25