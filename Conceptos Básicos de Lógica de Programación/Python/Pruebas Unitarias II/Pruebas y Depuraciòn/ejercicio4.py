# Se espera convertir una tupla a lista y agregar un nuevo elemento
tupla_original = (1, 2, 3)
lista_convertida = list(tupla_original)

lista_convertida.append[4]  # Error: append debería usar paréntesis ()

print(tuple(lista_convertida))
