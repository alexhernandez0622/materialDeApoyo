/*let numero = 7;
let esPar;

if (numero % 2 === 0) {
  esPar = true;
} else {
  esPar = false;
}

console.log(esPar ? "Es un número par" : "Es un número impar");*/

// Declaración de una variable llamada numero y asignación del valor 7
let numero = 7;

// Declaración de una variable llamada esPar que utiliza el operador ternario para determinar si numero es par o impar
let esPar = numero % 2 === 0 ? true : false;

// Impresión en la consola de un mensaje que depende del valor de la variable esPar
console.log(esPar ? "Es un número par" : "Es un número impar");

